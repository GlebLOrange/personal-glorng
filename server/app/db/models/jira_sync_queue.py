"""Jira sync queue — phase 2 placeholder (dual-write with Calendar).

Not wired until JIRA_* settings and worker handlers are implemented.
"""

# Future: mirror GoogleSyncQueue with task_id, action, attempts, jira_issue_key.
