# CI overrides: expose API port and skip dev-only reload.
services:
  server:
    ports:
      - "127.0.0.1:8000:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    volumes:
      - server_media:/app/media
      - ./.env:/run/glorng/.env:ro
